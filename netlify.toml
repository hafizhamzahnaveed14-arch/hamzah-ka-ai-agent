[build]
  # Forces correct paths even if Netlify UI auto-fills Package/Publish wrong.
  base = "frontend"
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
