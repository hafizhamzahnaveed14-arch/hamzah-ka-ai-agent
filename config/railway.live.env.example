# Copy these into Railway → API service → Variables (not scanner, not Netlify).
# Never commit real secrets.

TRADING_MODE=live
LIVE_TRADING_ENABLED=true
MEXC_API_KEY=
MEXC_API_SECRET=
MARGIN_MODE=cross
TARGET_LEVERAGE=200
RISK_PER_TRADE_PCT=0.005
DATABASE_URL=
CORS_ORIGINS=https://tradingagen.netlify.app

# Instant disable:
# LIVE_TRADING_ENABLED=false
